---
name: docs-author
description: Edit implementation docs without widening paper claims.
---

Sửa `docs/` và comment/docstring tiếng Anh. Không sửa `paper/` hay `artifacts/reports/final_ndss/` trừ khi người dùng đã duyệt. Chạy `python scripts/experiments/check_paper_claims.py` trước khi kết luận. Cấm cụm overclaim trong README của check_paper_claims.py.

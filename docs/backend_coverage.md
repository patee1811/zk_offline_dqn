# Backend Coverage

| Relation / component | Python semantic oracle | SP1 proof-backed | Current safe claim | Notes |
| --- | --- | --- | --- | --- |
| Dataset membership / Merkle membership | yes | yes for canonical leaf-hash membership vector and generated dataset-size scaling cases with verified proof provenance | SP1 proof-backed Merkle membership for canonical leaf hash against provenance-bound dataset root | Does not prove honest collection; Python computes the canonical transition hash before the SP1 leaf-hash membership proof. |
| Provenance-bound dataset commitment | yes | no | artifact-level binding from dataset root to manifest, audit report, raw trajectory, and collection-log hashes when available | Self-collected data can be replay/reward audited before commitment; public benchmark data remains source-integrity-only. |
| TD MVP canonical vector | yes | yes | SP1 proof generation and verification for the TD MVP canonical vector | Baseline fixture: `zk_backend/test_vectors/td_mvp_case_0.json`. |
| Distinct minibatch TD | yes | yes for TD-1, TD-2, TD-4, TD-8 cases | SP1 proof-backed for Distinct minibatch TD configurations | Supported with verified SP1 prove/verify benchmarks. |
| Forward-TD MLP | yes | yes for canonical tiny vector | SP1 proof-backed fixed-point Forward-TD MLP for canonical tiny vector | Not full training or backpropagation. |
| One-step SGD tiny | yes | yes for canonical vector | SP1 proof-backed tiny fixed-point SGD update | Not Adam or full optimizer-state correctness. |
| Short trace / checkpoint chaining | yes | yes for canonical tiny vector | SP1 proof-backed tiny checkpoint-chain validation | Not end-to-end DQN training from initialization to final checkpoint. |
| One-step training update | yes | yes for canonical batch-size-1 tiny MLP vector | SP1 proof-backed one-step training update with replay membership, forward TD, SmoothL1 loss, gradient/backprop, fixed-point SGD, and checkpoint transition | Not full training, Adam, recursion, model selection, or all replay batches. |
| Multi-step training fragment | yes | yes for canonical k={1, 4, 8} tiny MLP vectors | SP1 proof-backed multi-step training fragment with deterministic sampling, replay membership, forward TD, SmoothL1 loss, backprop, SGD, target sync, and checkpoint chaining | Not full training, Adam, all replay batches, model selection, or recursive aggregation. k={16, 32, 128} have execute-mode validation only from Phase 6 Kaggle runs. |
| Training-fragment chunk aggregation | yes | yes for proof-manifest chains with T={32, 64, 128} over k=8 chunk records | SP1 proof-backed chunk-chain aggregation over externally verified k=8 training-fragment proof manifests for W_0 -> W_T | Binds child proof-manifest hashes, aggregate roots, dataset/config consistency, and online/target checkpoint links. This mode hashes child manifests rather than verifying them in-guest; the separate `recursive_sp1` row below does verify them. |
| Phase 8.3 tamper rejection table | yes | summarizes existing proof-backed relations and provenance checks | Table 3 reports that supported tampered artifacts are rejected at dataset audit, commitment verification, Python semantic oracle, Rust execute, SP1/public-input binding layers | It is a security/tamper benchmark over current coverage; it does not prove full DQN training, honest public collection, Adam, batch-size scaling, or true recursion. |
| Phase 9 theorem/threat-model mapping | yes | no new backend coverage | Formal statements map each supported theorem to relations, backends/verifiers, tests, and Table 2/Table 3 evidence | Documentation and claim-checking layer only; it does not add proofs, benchmarks, or new cryptographic claims. |
| Phase 10 artifact reproducibility package | yes | no new backend coverage | `make reproduce-small` validates compact reports, SP1 provenance hashes, theorem mapping, and a tiny data-audit smoke path | Packaging layer only; heavy proof and benchmark reruns remain opt-in and do not expand supported claims. |
| Backpropagation | partial / relation-local | no | future work | No full backend proof for a small MLP backpropagation path. |
| Adam / optimizer-state proof | no | no | unsupported / future work | Current tiny update scope is simple SGD only. |
| Whole training run under one proof | yes | yes for 1248 steps on both environments and 4992 steps on lunarlander-random | a single root proof with step_start 0 and step_end T over a binary tree of 156-step leaves, each leaf bound to the committed dataset | The 4992-step root is committed provenance but is not a Table 2 row: it was built at a different path, and guest_elf_sha256 currently identifies the build rather than the source. Verification is 0.054 s regardless of T. |
| Self-collected replay/reward audit | yes | no | replay/reward audit before Merkle commitment | This is dataset provenance support, not SP1 proof coverage. |
| Public benchmark source integrity | yes | no | source-integrity commitment only | Minari/D4RL imports do not prove original honest collection. |
| Honest data collection | partial replay audit for self-collected data only | no | unsupported as a cryptographic honest-collection proof | The verifier checks membership after replay commitment. |
| Recursive aggregation | yes | yes for flat T={16, 32, 64}, binary tree T={16, 1248}, and Groth16 child proofs at T=16 | `recursive_sp1` mode verifies each child proof cryptographically inside the aggregate guest | CUDA prover only: peak 18.4 GB device memory, flat in T, and the CPU prover does not complete within 61 GB. Cost is about 154M cycles per child verified, independent of how many training steps that child covers. |

# Proof Statement Design

This file is kept as a compatibility pointer for older notes and links.

The canonical artifact schema and proof-statement cleanup notes now live in:

```text
docs/artifact_schema.md
```

Use that document for the current classification of:

- public inputs;
- private witness fields;
- debug/audit fields;
- one-step update artifacts;
- short-trace artifacts;
- backend-ready cleanup direction.

The current repository position remains:

```text
Python artifact/verifier prototype
-> backend-ready TD relation
-> SP1-backed TD MVP proof
-> later one-step, short-trace, and recursive proof work
```

This repository still does not contain a production zero-knowledge proof-of-training system.

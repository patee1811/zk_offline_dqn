"""Future package root for the Phase 1 refactor.

The active package remains the repository-root ``zk_offline_dqn`` package.
Modules under ``src/`` are compatibility wrappers until code is migrated in a
later phase.
"""
